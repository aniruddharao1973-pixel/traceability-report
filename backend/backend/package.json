{
  "name": "trace-backend",
  "version": "1.0.0",
  "description": "Traceability backend with Express + PostgreSQL",
  "main": "index.js",
  "scripts": {
    "prestart": "npx kill-port 4000 || exit 0",
    "start": "node index.js",
    "predev": "npx kill-port 4000 || exit 0",
    "dev": "nodemon --signal SIGTERM index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "nodemon": "^3.1.0"
  }
}
